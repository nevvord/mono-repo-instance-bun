{
  "name": "@mono-repo/core",
  "version": "1.0.0",
  "description": "Shared types, config, and utilities",
  "main": "index.ts",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
