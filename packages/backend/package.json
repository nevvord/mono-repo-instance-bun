{
  "name": "@mono-repo/backend",
  "version": "1.0.0",
  "description": "Backend API server with Hono",
  "main": "index.ts",
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "hono": "^4.1.0",
    "@mono-repo/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
